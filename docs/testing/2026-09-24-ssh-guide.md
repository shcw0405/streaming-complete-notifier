# SSH 向导 0.3 验收记录

- 桌面包 0.3.0：SSH 导航、真实剪贴板复制、步骤展开、手动确认保存及重载、窄窗口布局、隐藏 Dock 均通过。
- 原有自动化测试：15 项通过。
- 经用户授权连接 macmini：macOS、Codex CLI 0.156.1，已登录。
- SSH PTY 执行 `printf '\a'`：退出码 0，本地收到恰好一个 BEL 字节（十六进制 07）。这仅证明信号透传，不证明扬声器发声。
- 在 macmini 运行系统 Glass 音频：afplay 退出码 0，听觉确认待用户回复。这是远端扬声器测试，不是本地终端提醒。
- VS Code 已存在 macmini Remote SSH 窗口；原 Codex 会话显示额度耗尽，未完成新模型任务验收。
- 窗口自动化无法创建独立测试终端；未修改原会话、真实 VS Code 设置或远端 Codex 配置。
- SSH 非交互 shell 不含 Homebrew 路径；检查版本时临时增加 /opt/homebrew/bin，未写启动文件。

待完成：本地 VS Code 可闻响铃、远端 BEL 触发本地可闻响铃、真实 Codex 一轮完成及非焦点情况下提醒。不可把以上未完成项目标为已通过。
